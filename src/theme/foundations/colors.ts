import tailwindTheme from '../_generated/tailwindTheme';

export const colors = {
  ...(tailwindTheme.colors || {}),
  // secondaryGray: tailwindTheme.colors.secondaryGray || {},
  // brand: tailwindTheme.colors.teal || {},
  // brandScheme: tailwindTheme.colors.teal || {},
  // brandTabs: tailwindTheme.colors.teal || {},
};
