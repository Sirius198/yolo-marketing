/**
 * This template is for "How it works" page
 * 1. Main Landing Page
 * 2. Doctor landing page
 */

type SectionTemplate3Props = {};

const defaultProps: SectionTemplate3Props = {};

const SectionTemplate3: React.FC<SectionTemplate3Props> = ({}) => {
  return <section className="w-full py-8 lg:py-[100px]"></section>;
};

SectionTemplate3.defaultProps = defaultProps;

export default SectionTemplate3;
